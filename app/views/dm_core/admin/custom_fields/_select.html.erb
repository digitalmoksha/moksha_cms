<%= markdown(f.object.description) unless f.object.description.blank? %>
<%= f.input :field_data, as: :select, collection: f.object.choice_list_array, label: field.label.blank? ? false : field.label, required: field.required? %>
<%= f.hidden_field :custom_field_def_id %>
