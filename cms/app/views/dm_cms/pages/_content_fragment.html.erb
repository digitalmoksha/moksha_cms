<%#--- leave formatting alone - prefer final html to be as natural as possible %>
<% if is_admin? %>
<div class="fragment" id="fragment_<%= content_fragment.id %>">
<%= render_content_item(content_fragment) %>
</div>
<% elsif content_fragment.enable_cache %>
  <% cache([Account.current.id, DmCore::Language.locale, content_fragment]) do %>
<%= render_content_item(content_fragment) %>
  <% end %>
<% else %>
<%= render_content_item(content_fragment) %>
<% end %>
