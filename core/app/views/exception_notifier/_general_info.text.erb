* User           : <%= @user.nil? ? "Anonymous" : "#{h(@user.email)} -- #{h(@user.display_name)} -- (id: #{h(@user.id)})" %>
* Account ID     : <%= @account.nil? ? 'n/a' : @account.id %>
* Account Domain : <%= @account.nil? ? 'n/a' : @account.domain %>
* Account Prefix : <%= @account.nil? ? 'n/a' : @account.account_prefix %>